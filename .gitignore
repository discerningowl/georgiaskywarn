# macOS system files
.DS_Store
**/.DS_Store

# Test and backup files
ACTIVATION_PATTERN_TESTS.md
data/linked-repeaters.json.backup
data/nonlinked-repeaters.json.backup

# Local environment / secrets — never commit
.env
*.env
.env.local
config.local.json
